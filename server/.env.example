# PostgreSQL Database Configuration
# Copy this file to .env and fill in your actual credentials
# DO NOT commit .env to version control!

DB_HOST=localhost
DB_PORT=5432
DB_NAME=edgedevices
DB_USER=iot_user
DB_PASSWORD=your_secure_password_here

# MQTT Broker Configuration (optional, defaults to 127.0.0.1:1883)
# MQTT_BROKER=127.0.0.1
# MQTT_PORT=1883

# Flask Server Configuration (optional, defaults to 0.0.0.0:8000)
# FLASK_HOST=0.0.0.0
# FLASK_PORT=8000
